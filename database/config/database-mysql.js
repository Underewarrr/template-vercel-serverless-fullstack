const config = {
    host: '127.0.0.1',
    user: 'db_pxa', // replace with your MySQL username
    password: 'G4cPf8Ap8tdeiyWB', // replace with your MySQL password
    database: 'db_pxa', // replace with the name of your MySQL database
    logging: true
};
  
  module.exports = config;
  